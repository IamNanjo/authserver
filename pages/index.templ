package pages

templ Index() {
{{
var pageTitle = "Authentication service"
var pageStyle = "index.css"
var pageScript = ""
}}
@Layout(pageTitle, pageStyle, pageScript) {
<main class="center">Authentication service</main>
}
}
